---
kind: design
status: agreed
created: {{created}}
---

# {{Name}}

{{purpose}}

## How it works

The current shape — components, seams, data flow. Present tense, describing what *is*, not what was decided.

## Why this shape

The constraints that force it. Enough that a reader can tell a safe change from a breaking one.

## Boundaries

What this deliberately does not cover, and where that lives instead.

<!-- Re-true this file in place as the system changes; never append a changelog.
     status: agreed → active → implemented → superseded.
     When superseded, add `superseded_by: <path>` to the frontmatter and leave the body alone. -->
